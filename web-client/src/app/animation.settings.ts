const transitionMs = 200;

export const DefaultTransitions = {
  linear: transitionMs,
  easeIn: `${transitionMs}ms ease-in`,
  easeOut: `${transitionMs}ms ease-out`,
  easeBoth: `${transitionMs}ms ease-in-out`
};
