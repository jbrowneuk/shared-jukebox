export interface TrackData {
  title: string;
  album: string;
  artist: string;
  songId: string;
  lengthMs: number;
}

export interface ClientData {
  name: string;
  supportedApiVersion: number;
}
